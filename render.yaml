# render.yaml
services:
  - type: web
    name: mine606
    env: python
    plan: starter
    region: oregon
    buildCommand: |
      pip install -r requirements.txt
    startCommand: uvicorn app.main:app --host 0.0.0.0 --port 10000
    autoDeploy: true
